mixin card(img, alt, title, updated)
  .card
    img.card-img-top(src=img, alt=alt)
    .card-body
      h5.card-title= title
      if block
        block
      else
        p.card-text
      p.card-text.text-right
        small.text-muted Last updated #{updated} ago
